<UserControl x:Class="RecorderUI.Component.FormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecorderUI.Component"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ControlTemplate
                x:Key="FormTemplate"
                TargetType="UserControl">
                <StackPanel
                    Orientation="Horizontal">
                    <TextBlock
                        Height="20"
                        Width="{Binding labelWidth}"
                        Margin="5"
                        Text="{Binding displayName}"/>
                    <ContentPresenter
                        Margin="5"/>
                </StackPanel>
            </ControlTemplate>

            <DataTemplate
                DataType="{x:Type local:TextForm}">
                <UserControl
                    Template="{StaticResource FormTemplate}">
                    <TextBox
                        Height="20"
                        Width="300"
                        Text="{Binding input}"/>
                </UserControl>
            </DataTemplate>

            <DataTemplate
                DataType="{x:Type local:ComboForm}">
                <UserControl
                    Template="{StaticResource FormTemplate}">
                    <ComboBox
                        Height="20"
                        Width="300"
                        ItemsSource="{Binding listItem, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                        SelectedItem="{Binding input}"/>
                </UserControl>
            </DataTemplate>

            <DataTemplate
                DataType="{x:Type local:PathForm}">
                <UserControl
                    Template="{StaticResource FormTemplate}">
                    <StackPanel
                        Orientation="Horizontal">
                        <TextBox
                            Height="20"
                            Width="300"
                            IsEnabled="False"
                            Text="{Binding input}"/>
                        <Button
                            Margin="10 0 0 0"
                            Height="20"
                            Width="100"
                            Content="Search"
                            Command="{Binding addPathCommand}"/>
                    </StackPanel>
                </UserControl>
            </DataTemplate>

            <DataTemplate
                DataType="{x:Type local:IntForm}">
                <UserControl
                    Template="{StaticResource FormTemplate}">
                    <TextBox
                        Height="20"
                        Width="300"
                        Text="{Binding input}"/>
                </UserControl>
            </DataTemplate>

            <DataTemplate
                DataType="{x:Type local:DecimalForm}">
                <UserControl
                    Template="{StaticResource FormTemplate}">
                    <TextBox
                        Height="20"
                        Width="300"
                        Text="{Binding input}"/>
                </UserControl>
            </DataTemplate>

            <DataTemplate
                DataType="{x:Type local:DateForm}">
                <UserControl
                    Template="{StaticResource FormTemplate}">
                    <DatePicker
                        SelectedDate="{Binding input}"/>
                </UserControl>
            </DataTemplate>

            <DataTemplate
                DataType="{x:Type local:CheckBoxForm}">
                <UserControl
                    Template="{StaticResource FormTemplate}">
                    <CheckBox
                        IsChecked="{Binding input}"/>
                </UserControl>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel
        Orientation="Vertical">
        <ItemsControl
            ItemsSource="{Binding listFormWrapper}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ItemsControl
                        Margin="0 0 0 20"
                        ItemsSource="{Binding listForm}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <Button
                Height="20"
                Width="100"
                Margin="5"
                Visibility="{Binding addItemVisibility}"
                Content="Add item"
                Command="{Binding addItemCommand}"/>
            <Button
                Height="20"
                Width="100"
                Margin="5"
                Visibility="{Binding insertVisibility}"
                Content="Insert"
                Command="{Binding insertCommand}"/>
            <Button
                Height="20"
                Width="100"
                Margin="5"
                Visibility="{Binding applyVisibility}"
                Content="Apply"
                Command="{Binding applyCommand}"/>
            <Button
                Height="20"
                Width="100"
                Margin="5"
                Content="Reset"
                Command="{Binding resetCommand}"/>
        </StackPanel>
    </StackPanel>
</UserControl>