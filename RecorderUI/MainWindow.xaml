<Window x:Class="RecorderUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RecorderUI"
        xmlns:view="clr-namespace:RecorderUI.View"
        xmlns:viewModel="clr-namespace:RecorderUI.ViewModel"
        mc:Ignorable="d"
        Title="FlaUI recorder" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <StackPanel>
        <ContentControl Content="{Binding currentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type viewModel:ConfigViewModel}">
                    <view:ConfigView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewModel:RecordViewModel}">
                    <view:RecordView/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </StackPanel>
</Window>
